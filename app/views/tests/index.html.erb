<div class="row justify-content-center">
  <h1>
    <%= t('.tests') %>
  </h1>
</div>

<div class="row col-md-6 offset-md-3 table-responsive-sm">

  <table class="table table-striped">
    <thead class="thead-dark">
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.question') %></th>
        <th><%= t('.action') %></th>
      </tr>
    </thead>

    <%= render @tests %>
  </table>
</div>
